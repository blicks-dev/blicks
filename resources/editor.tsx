// Registers WordPress' core inline RichText formats (text/highlight color, strikethrough,
// subscript, superscript, …) so blocks can opt into them via `allowedFormats`. Adds the
// `wp-format-library` script dependency, so the formats are available in the editor.
import './block-library';
import './theme-settings';
import './import';
