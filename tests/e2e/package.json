{
	"name": "blicks-e2e",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "End-to-end suite. Isolated from the plugin package because only this needs Playwright and the Playground CLI, so the build and release jobs never install them.",
	"scripts": {
		"test": "playwright test",
		"test:ui": "playwright test --ui",
		"report": "playwright show-report"
	},
	"devDependencies": {
		"@playwright/test": "^1.62.1",
		"@wp-playground/cli": "^3.1.52"
	},
	"pnpm": {
		"overrides": {
			"qs@>=2.2.5 <6.16.0": ">=6.16.0",
			"path-to-regexp@<0.1.13": ">=0.1.13"
		}
	}
}
